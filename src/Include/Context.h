#pragma once

// Used so a button takes a context as a parameter (Game, GameRun or Encounter)
// This way a button can call functions from a context without creating several buttons classes for each
class Context
{

};